import { MakeGreyDirective } from './make-grey.directive';

describe('MakeGreyDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeGreyDirective();
    expect(directive).toBeTruthy();
  });
});
